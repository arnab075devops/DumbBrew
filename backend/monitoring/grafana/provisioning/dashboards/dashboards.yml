apiVersion: 1

providers:
  - name: dumbbrew
    orgId: 1
    folder: DumbBrew
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
