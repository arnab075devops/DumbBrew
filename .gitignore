node_modules/
dist/
*.log
.env
.env.*
!.env.example
rzp-key.csv
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
terraform.tfvars
*.pem
*.key

# Raw/unoptimized source images (100MB+) — not used by the site. The
# web-ready compressed copies actually served live in
# backend/gateway/public/assets/ and project/assets/ (both tracked).
/asset/
